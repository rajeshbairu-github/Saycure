<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.saycure">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SayCure.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".BookingProcess"
            android:theme="@style/Theme.SayCure.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Search_Hospitals">

        </activity>
        <activity android:name=".Search_Hospital_byLocation">

        </activity>
        <activity android:name=".SplashScreen" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/Theme.SayCure.NoActionBar" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".VerifyPhoneActivity" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>